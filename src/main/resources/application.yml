debug: false #debug ?? // ?? ?? ??? ???? false ???? logging?? ??? ??? debug???????.

management:
  endpoints:
    web:
      exposure:
        include: "*" # actuator ? ??? ?? ??? ??? ???? ?

logging:
  level:
    com.example.com.project.project_initializr: debug #???? ??? ?????
    org.springframework.web.servlet : debug
    org.hibernate.SQL: trace
    org.hibernate.type: trace
    org.hibernate.orm.jdbc.bind: trace

spring:
  datasource: # db ?? ??
    url: jdbc:mysql://localhost:3306/board
    username: johnny
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:h2:mem:testdb #h2? ?? ???? ???? ?? ?
#    username: sa
#    driver-class-name: org.h2.Driver
  jpa:
    defer-datasource-initialization: true #resources ?? ?? data.sql ??? ??? ???? db ???? ??? ? ?
    hibernate:
      ddl-auto: create  # entity? ???? ddl ????
    show-sql: true
    properties:
      hibernate:
        format_sql: true #???? sql ??? ?? ?? ??
        default_batch_fetch_size: 100 # ????? ?? ???? ??? ??? ??? ????.
#    open-in-view: false #OSIV ??
#  h2:
#    console:
#      enabled: false # h2 disable
  sql:
    init:
      mode: always #resources ?? ?? data.sql ? ?? ????? ???? ??
  data.rest:
      base-path: /api
      detection-strategy: annotated # ?????(@RepositoryRestResource)?? ??? ??????
#  thymeleaf3:
#    decoupled-logic: true



---
# --- ??? ??? yml ??? ??
#spring:
#  config:
#    activate:
#      on-profile: testDb
#  datasource: # db ?? ??
#    url: jdbc:h2:mem:board;mode=mysql
#    driver-class-name: org.h2.Driver
#  sql:
#    init:
#      mode: always
#  test.database.replace:none